import React from 'react';
import { ControlRef } from '@hubfx/forms';

export interface FormArrayProps {
  component: React.JSXElementConstructor<unknown>;
  controlRef: ControlRef;
}

export const FormArray = ({ controlRef }) => {
  return <div></div>;
};
